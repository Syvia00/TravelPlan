namespace TravelPlan.Shared.Models.Enums;

/// <summary>Shared by TripCollaborators and TripShareLinks — both use the same Editor/Viewer role set.</summary>
public enum TripRole
{
    Viewer,
    Editor
}
